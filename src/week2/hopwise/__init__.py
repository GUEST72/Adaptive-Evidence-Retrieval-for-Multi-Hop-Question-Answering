"""Week 2 Task 2: oracle hop-wise retrieval.

Retrieves evidence once per reasoning hop using the gold decomposition, so the
contribution of hop-conditioned retrieval is measured without contamination from
generated-decomposition errors.
"""
